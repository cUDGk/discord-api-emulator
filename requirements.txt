fastapi>=0.115
uvicorn[standard]>=0.32
websockets>=13
pydantic>=2.9
jinja2>=3.1
python-multipart>=0.0.12
Pillow>=10.0
# Optional: ETF encoding (?encoding=etf) and zstd-stream compression.
# The gateway falls back to pure-python ETF and disables zstd if these are absent.
erlpack>=0.4 ; python_version < "3.99"
zstandard>=0.22
